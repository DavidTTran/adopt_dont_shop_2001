<h2>Pets Index</h2>

<% @pets.each do |pet| %>
  <img src="<%= pet.image %>" width="200" height="200">
  <p>Name: <%= pet.name %></p>
  <p>Sex: <%= pet.sex %></p>
  <p>Approximate Age: <%= pet.approximate_age %></p>
<% end %>
